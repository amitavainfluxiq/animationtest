{
  "name": "animation_test",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "s3-sync":"aws --profile lambda_all s3 sync ./ s3://demo-proj"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "three": "^0.162.0"
  }
}
